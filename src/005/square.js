function area(a) {
    let areaSquare = a * a
    return areaSquare;
}

function perimeter(a) {
    let perimeterSquare = 4 * a
    return perimeterSquare;
}

module.exports = {
    area, perimeter
};